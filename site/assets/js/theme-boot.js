/* generated by tools/build_content.py - do not edit; sets the background before first paint */
(function () {
  try {
    var def = "terminal", locked = true, pick = def;
    if (!locked) {
      var u = new URLSearchParams(location.search).get('theme'), s = null;
      try { s = localStorage.getItem('theme'); } catch (e) { /* storage blocked */ }
      var ok = function (v) { return v === 'paper' || v === 'terminal'; };
      pick = ok(u) ? u : ok(s) ? s : def;
    }
    if (pick === 'terminal') {
      document.documentElement.style.background = "#000000";
      document.documentElement.classList.add('pre-terminal');
    }
  } catch (e) { /* never block the page */ }
})();
